---
title: "WordPress"
description: "The `settings` object for `wordpress` channels on POST /posts."
---

Channels whose `identifier` is `wordpress` validate the `settings` object below.
This page is generated from the application's own validation layer, so it
matches exactly what the server accepts.

Set `settings.__type` to `"wordpress"`.

## Fields

| Field | Type | Required | Notes |
|---|---|---|---|
| `title` | string | **required** |  |
| `main_image` | object | optional |  |
| `type` | string | **required** |  |
| `categories` | number | optional |  |
| `tags` | number | optional |  |
| `status` | enum | optional | Accepted: `publish`, `draft`, `pending`, `private`. |

<Note>
Settings are validated when a post is scheduled or published. Drafts skip this
validation entirely - see [errors](/public-api/errors) for how failures are
reported.
</Note>
