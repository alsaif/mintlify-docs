---
title: "ListMonk"
description: "The `settings` object for `listmonk` channels on POST /posts."
---

Channels whose `identifier` is `listmonk` validate the `settings` object below.
This page is generated from the application's own validation layer, so it
matches exactly what the server accepts.

Set `settings.__type` to `"listmonk"`.

## Fields

| Field | Type | Required | Notes |
|---|---|---|---|
| `subject` | string | **required** |  |
| `preview` | string | **required** |  |
| `list` | string | **required** | List must be an id |
| `template` | string | optional | Template must be an id |

<Note>
Settings are validated when a post is scheduled or published. Drafts skip this
validation entirely - see [errors](/public-api/errors) for how failures are
reported.
</Note>
