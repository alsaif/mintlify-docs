---
title: "Pinterest"
description: "The `settings` object for `pinterest` channels on POST /posts."
---

Channels whose `identifier` is `pinterest` validate the `settings` object below.
This page is generated from the application's own validation layer, so it
matches exactly what the server accepts.

Set `settings.__type` to `"pinterest"`.

## Fields

| Field | Type | Required | Notes |
|---|---|---|---|
| `title` | string | optional |  |
| `link` | string (URL) | optional |  |
| `dominant_color` | string | optional |  |
| `board` | string | **required** | board must be an id |

<Note>
Settings are validated when a post is scheduled or published. Drafts skip this
validation entirely - see [errors](/public-api/errors) for how failures are
reported.
</Note>
